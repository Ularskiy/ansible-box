- name: Проверка переменной vm_list
  assert:
    that:
      - vm_list is defined
      - vm_list | length > 0
    fail_msg: "vm_list не задан или пуст!"
  tags: [validate]

- name: Валидация узла
  import_tasks: validate_node.yml
  tags: [validate]

- name: Проверка существующих ВМ
  import_tasks: check_vm_exists.yml
  tags: [check, clone, delete]

# 👇 ВРЕМЕННО 
- name: Debug — вывод полной структуры check_vm_result
  debug:
    var: check_vm_result
  tags: [debug]

- name: Debug — статус существования ВМ из check_vm_result
  debug:
    msg: "ВМ {{ item.item.name }} → status: {{ item.status }}"
  loop: "{{ check_vm_result.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  tags: [debug]

- name: Удалить ВМ
  import_tasks: delete.yml
  when:
    - item.state == 'absent'
    - check_vm_result.results is defined
    - check_vm_result.results[loop.index0].status == 200
  tags: [delete]

- name: Клонировать ВМ
  include_tasks: clone.yml
  loop: "{{ check_vm_result.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  when:
    - item.item.state in ['present', 'started']
    - item.status == 404
  tags: [clone]

- name: Debug — ВМ уже существует, пропускаем клонирование
  debug:
    msg: "Пропущено: ВМ {{ item.item.name }} уже существует (ID {{ item.item.id }})"
  loop: "{{ check_vm_result.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  when:
    - item.item.state in ['present', 'started']
    - item.status == 200
  tags: [clone]

- name: Подождать снятие lock
  include_tasks: wait_unlock.yml
  loop: "{{ check_vm_result.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  when:
    - item.item.state in ['present', 'started']
    - item.status == 404  # только если ВМ действительно была клонирована
  tags: [wait, clone]

- name: Настроить параметры Cloud-Init
  include_tasks: cloud_init.yml
  loop: "{{ vm_list }}"
  loop_control:
    label: "{{ item.name }}"
  when: item.state in ['present', 'started']
  tags: [cloud_init]

- name: Запустить ВМ
  import_tasks: start.yml
  when: item.state == 'started'
  tags: [start]

- name: Остановить ВМ
  import_tasks: stop.yml
  when: item.state == 'stopped'
  tags: [stop]

- name: Сделать снапшот
  import_tasks: snapshot.yml
  when: item.state == 'snapshot'
  tags: [snapshot]

- name: Сделать бэкап
  import_tasks: backup.yml
  when: item.state == 'backup'
  tags: [backup]

